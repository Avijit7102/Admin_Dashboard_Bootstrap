<!-- 
* Navbar Layout:
    * .navbar-brand – Positioned on the left; contains the icon and “Admin Dashboard” text.
    * .navbar-nav.ms-auto – Positioned on the right; contains navigation links.
    * .d-flex.align-items-center – Vertically centers the icon and text within the brand.
    * .fs-4 – Sets a consistent font size for the icon to match the brand text.
    * .me-2 – Adds spacing between the icon and the brand text.
    * The JS script enables the hamburger menu to expand/collapse on mobile devices.

* Spacing Utilities:
    * .py-5 – Adds vertical padding (top and bottom); 'p' = padding, 'y' = vertical axis, '5' = largest spacing.
    * .g-4 – Adds gutter spacing between grid items (e.g., cards).

* Table Styling:
    * .table-striped – Adds alternating row colors for better readability.
    * .table-hover – Adds a hover effect on rows.
    * .table-responsive – Makes the table scrollable on smaller screens.
    * .align-middle – Vertically centers the content within table cells.
    * .table-dark – Applies a dark background to the table header (<thead>).
    * .fw-bold – Makes status text bold for emphasis.
-->
